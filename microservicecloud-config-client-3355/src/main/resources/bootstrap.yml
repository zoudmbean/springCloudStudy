spring:
  cloud:
    config:
      name: microservicecloud-config-client  # 需要从github上读取的资源名称（也就是配置文件的名称），注意没有yml后缀名
      profile: test                    # 本次访问的配置项
      labler: master
      uri: http://config-3344:3344    # 本微服务启动后先去查找3344号服务，通过SpringCloudConfig获取gitHub的服务地址